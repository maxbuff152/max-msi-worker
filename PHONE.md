# Phone → MSI labels

Account: `info@sellersfirsthomesolutions.com`  
Runtime: **My Machines** (never Cloud).

## Pick the label that matches the job

| Want to work on… | Pick this |
|------------------|-----------|
| Sellers First website | **MSI Website** |
| This PC / worker scripts | **MSI Infra** |
| Mac texts / email notify | **MSI Messages** |

Pull down to refresh the machine list after a restart. The old **Max-MSI** (website-only) row should be gone.

## Quick fleet proof (optional)

```
On MSI Website or MSI Infra: ssh macbook with echo MAC_OK and hostname, then ssh logan with echo LOGAN_OK and hostname. Report both. Do not sign those PCs out of Cursor.
```

Lenovo SSH is Windows cmd: chain with `&`, not bash `;`.

Mac/Lenovo stay on Maxwell Cursor. MSI stays info@. Keep MSI awake.
